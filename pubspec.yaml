name: haskell_is_beautiful
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_highlight: ^0.6.0
  share: ^0.6.4+2
  path_provider: ^1.6.8
  cupertino_icons: ^0.1.3
  grouped_list: ^3.1.0
  recase: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/Code/
    - assets/Code/Algorythms/
    - assets/Code/Algorythms/Merge Sort/
    - assets/Code/Beginning/
